# Ignore generated files/directories
Bin/
obj/
*.zip
KerbalSimPit.version
Properties/*.cs
.vs
releases/*

# Ignore the symlink files, pointing to the game install directory
install
KerbalSpaceProgram
